# Build-time tooling for regenerating the World Magnetic Model lookup
# tables (src/wmm_lut.h, src/wmm_test_vectors.h). NOT a runtime
# dependency of the INSLIB package or python/replay.py -- only needed to
# run `make wmm` when the WMM epoch changes or the model is updated.
#   pip install -r magneticmodel/requirements.txt
pygeomag>=1.1

# wmm_error_analysis.py only (measures the shipped table against the exact
# spherical-harmonics model). matplotlib is needed for its --plot option.
numpy>=1.24
matplotlib>=3.5
